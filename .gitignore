# Secrets — never commit these
.env
config/google_service_account.json

# User data — don't track in git (except seed files below)
data/*.json
data/*.csv

# Keep seed data so Streamlit Cloud starts with content
!data/cultural_calendar.json

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Logs
*.log
/tmp/

# Virtual environments
venv/
.venv/
env/
