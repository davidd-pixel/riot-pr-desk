# Secrets — never commit these
.env
config/google_service_account.json

# User data — tracked in git so Streamlit Cloud always has a baseline.
# Drive persistence (drive_persistence.py) keeps Cloud data current between deploys.
data/*.csv

# Never commit raw feedback noise
# data/feedback.json  ← keep this tracked too so votes persist

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Logs
*.log
/tmp/

# Virtual environments
venv/
.venv/
env/
